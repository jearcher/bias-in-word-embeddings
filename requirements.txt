# This is everything from Josh's GCP VM using `pip freeze > requirements.txt`
# Install with `pip install -r requirements.txt` in the command line

gensim>=4.0.0
numpy>=1.18.5
json>=2.0.9
scipy>=1.4.1




