# This is everything from Josh's GCP VM using `pip freeze > requirements.txt`
# Install with `pip install -r requirements.txt` in the command line

gensim>=4.0.0
numpy>=1.18.5
json>=2.0.9
scipy>=1.4.1

# Progress bar in jupyter nb
#tqdm >= 4.47.0
#ipywidgets
# May have to run this line:
# jupyter nbextension enable --py widgetsnbextension
# docs: https://ipywidgets.readthedocs.io/en/latest/user_install.html



